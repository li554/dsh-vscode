feat: 0.2.53 - replace @anionex/dsh-turn-rewind with dsh-undo-plugin

Fast, bounded conversation rollback via @dsh-undo (23swccp/dsh-undo).

- Replace @anionex/dsh-turn-rewind with dsh-undo-plugin (npm prebuilt rc.8, 8 packages: bundle + 7 @dsh-undo/*)
- dsh-undo: /undo command + row/header rollback buttons, fork-to-new-session on rollback (model never sees reverted prompts), archive-tasks manager in settings, shadow-Git file restore that never touches the project's .git; ~500ms rollback on 7400+ file workspaces
- Ship prebuilt npm tarballs into plugins/bundled (no source build: it needs pnpm + typert-generator, blocked by offline/no-pnpm constraint); peer deps resolve from vendored @deepseek-ai/dsh-*@0.1.1-rc.2 (satisfies ^0.1.0-rc.6)
- Register dsh-undo-plugin in BUNDLED_PLUGINS; retire @anionex/dsh-turn-rewind (RETIRED_PLUGINS) so 0.2.52 profiles auto-prune the old transplant + manifest entry; remove plugins/bundled/@anionex
- Sync smoke/discover roots (_selfcontained.mjs, boot-test.mjs, discover-entries.cjs, _closure.mjs, selfcontained-plugins.mjs, _fullcopy-boot.mjs), vsixmanifest, README
- Bump version to 0.2.53

Verified: boot-test mounts all 4 @dsh-undo client bundles (rollback-button/settings/toolcards/trailfold) into the web-client inject roster with no resolve errors; packed 98.18MB vsix and installed.